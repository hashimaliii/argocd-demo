apiVersion: v1
kind: Service
metadata:
  name: argocd-demo-svc
  namespace: default
spec:
  selector:
    app: argocd-demo
  ports:
  - port: 80
    targetPort: 80
    nodePort: 30080
  type: NodePort
